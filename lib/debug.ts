/**
 * Development / visitor mode switch.
 *
 * When true (and Tracking Info is open), technical visualizations are drawn:
 * face mesh, direction label, and pose signal graph.
 *
 * Set to false for the normal visitor experience (stats + Recalibrate only).
 */
export const DEBUG = true;
